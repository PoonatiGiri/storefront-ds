---
title: Dashboard
---

import { MetricCard } from '../../../src/components/organisms/MetricCard/MetricCard';
import { StatusBadge } from '../../../src/components/molecules/StatusBadge/StatusBadge';
import { Avatar } from '../../../src/components/atoms/Avatar/Avatar';
import { Button } from '../../../src/components/atoms/Button/Button';
import { Preview } from '../../components/Preview';

# Dashboard

A merchant's home view — KPIs at the top, recent activity below. Built from `MetricCard`, `StatusBadge`, `Avatar`, `Button`.

## KPI strip

<Preview padded>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: 16, width: '100%' }}>
    <MetricCard title="Total revenue" value="$24,530" trend="up" trendValue="12.5%" period="vs last month" size="sm" />
    <MetricCard title="Orders" value="186" trend="up" trendValue="8.2%" period="vs last month" size="sm" />
    <MetricCard title="Avg order value" value="$131.88" trend="neutral" trendValue="0.4%" period="vs last month" size="sm" />
    <MetricCard title="Conversion" value="3.2%" trend="down" trendValue="0.8%" period="vs last month" size="sm" />
  </div>
</Preview>

## Recent orders

<Preview padded>
  <div style={{ width: '100%', background: 'white', border: '1px solid var(--storefront-color-surface-border)', borderRadius: 8, overflow: 'hidden' }}>
    <div style={{ padding: 16, borderBottom: '1px solid var(--storefront-color-surface-border)', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
      <h3 style={{ fontSize: 16, fontWeight: 600, margin: 0 }}>Recent orders</h3>
      <Button variant="ghost" size="sm">View all →</Button>
    </div>
    <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
      <thead>
        <tr style={{ background: 'var(--storefront-color-neutral-50)', textAlign: 'left' }}>
          <th style={{ padding: '10px 16px', fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em', color: 'var(--storefront-color-neutral-600)' }}>Order</th>
          <th style={{ padding: '10px 16px', fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em', color: 'var(--storefront-color-neutral-600)' }}>Customer</th>
          <th style={{ padding: '10px 16px', fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em', color: 'var(--storefront-color-neutral-600)' }}>Status</th>
          <th style={{ padding: '10px 16px', fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em', color: 'var(--storefront-color-neutral-600)', textAlign: 'right' }}>Total</th>
        </tr>
      </thead>
      <tbody>
        {[
          { id: '#ORD-1024', name: 'Sarah Johnson', initials: 'SJ', status: 'processing', total: '$207.50' },
          { id: '#ORD-1023', name: 'Marcus Lee',    initials: 'ML', status: 'completed',  total: '$249.00' },
          { id: '#ORD-1022', name: 'Priya Singh',   initials: 'PS', status: 'pending',    total: '$89.50' },
          { id: '#ORD-1021', name: 'Hugo Bernal',   initials: 'HB', status: 'failed',     total: '$314.75' },
          { id: '#ORD-1020', name: 'Jin Park',      initials: 'JP', status: 'cancelled',  total: '$159.99' },
        ].map((row) => (
          <tr key={row.id} style={{ borderTop: '1px solid var(--storefront-color-neutral-200)' }}>
            <td style={{ padding: '12px 16px', fontWeight: 600 }}>{row.id}</td>
            <td style={{ padding: '12px 16px' }}>
              <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
                <Avatar size="xs" initials={row.initials} />
                {row.name}
              </span>
            </td>
            <td style={{ padding: '12px 16px' }}>
              <StatusBadge status={row.status} size="sm" />
            </td>
            <td style={{ padding: '12px 16px', textAlign: 'right', fontVariantNumeric: 'tabular-nums', fontWeight: 500 }}>{row.total}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</Preview>

This snippet uses raw HTML/CSS instead of the full `DataTable` component to keep the example self-contained. In production, drop in the [`DataTable`](/components/data-table) for sorting, pagination, search, and selection out of the box.
