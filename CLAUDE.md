# Storefront DS — Design System

B2B SaaS / Commerce design system built in React + TypeScript + Tailwind + Storybook + Nextra docs.

**Live URLs**
- **Public docs site**: https://storefront-ds.vercel.app (Nextra + Vercel, auto-deploys on push to `main`)
- **Storybook**: https://poonatigiri.github.io/storefront-ds (auto-deploys on push to `main`)
- **GitHub**: https://github.com/PoonatiGiri/storefront-ds
- **Figma source of truth**: https://www.figma.com/design/2P3ina6EjiCFUORJtD56hE
- **Author**: Giri Poonati — https://www.linkedin.com/in/giri-poonati/

---

## Project status (as of 2026-05-15)

### Component count: 36

- **Atoms (12)**: Avatar, Badge, Button, Checkbox, Input, ProgressBar, Radio, Skeleton, Spinner, Switch, Tag, Tooltip
- **Molecules (15)**: Alert, AmountDisplay, Breadcrumb, DatePicker, Dropdown, EmptyState, FormField, LineItem, Pagination, PriceDisplay, SearchBar, Stepper, StatusBadge, Tabs, Toast
- **Organisms (9)**: DataTable, FilterPanel, MetricCard, Modal, OrderCard, OrderSummary, ProductCard, Sidebar, TopBar

Every component ships with: `Component.tsx` · `Component.stories.tsx` · `Component.figma.tsx` (Code Connect, placeholder node-ids on the newer batch) · `Component.mdx` (Storybook docs) · `index.ts` barrel.

### Public documentation site (Nextra v3 / Pages Router)

Lives at `docs/`. Pages:

- `/` — landing (hero + live ProductCards + tokens demo)
- `/use-cases` — 6 audience personas, what-you-can-build, why-teams-choose pillars, status checklist
- `/install` — getting started, theming, framework notes
- `/tokens` — interactive color / spacing / typography / radius / shadow browsers reading from `src/tokens/*.ts`
- `/principles` — 10 design + engineering principles
- `/components/*` — one MDX page per component (36 pages)
- `/patterns/{checkout,order-detail,dashboard}` — composed end-to-end flows
- `/theming` — live playground with 5 presets + color pickers + radius slider + auto-generated CSS snippet

Total: 52 static pages generated by `next build`.

### Build / CI / deploy state

- `npx tsc -b` — passes cleanly across the entire repo, zero errors
- `npm run build` (docs) — passes, 52 static pages
- GitHub Pages workflow — auto-deploys Storybook on every push to `main`
- Vercel — auto-deploys docs site on every push to `main`; preview URLs for every PR
- Chromatic workflow — configured but **missing `CHROMATIC_PROJECT_TOKEN` secret** in GitHub; fails on every push. Either add the token (chromatic.com → Manage → copy → GitHub repo secrets) or delete `.github/workflows/chromatic.yml`.

### Sessions log (chronological)

**Session 1 (2026-04-22-ish, summarized in prior memory)** — Initial build: 4 atoms + 4 molecules + 6 organisms + 123 Figma variables + 81 stories + Code Connect + 3 AI agents.

**Session 2 (2026-05-14)** — Doubled the component count + added public docs site:
1. Built **14 new components**: 8 atoms (Spinner, Skeleton, Tag, Checkbox, Radio, Switch, ProgressBar, Tooltip), 5 molecules (Alert, Toast, Tabs, Breadcrumb, EmptyState), 1 organism (Modal).
2. Built **8 Tier 1 commerce components**: AmountDisplay (canonical currency renderer), StatusBadge (5 mandated statuses), PriceDisplay, Stepper, DatePicker, LineItem, ProductCard, OrderSummary.
3. Fixed pre-existing TS errors across older components (Button, Input, Dropdown, FormField, Pagination, OrderCard, etc.) so `tsc -b` is fully green.
4. Resolved `OrderStatus` duplication — OrderCard now consumes the canonical type from StatusBadge.
5. Deleted leftover Vite-scaffold `src/components/Button/` directory.

**Session 3 (2026-05-15)** — Public docs site:
1. Scaffolded **Nextra v3** at `docs/` (Next.js 14 + Pages Router + Tailwind v4 + React 18).
2. Wrote landing + use-cases + install + tokens + principles pages.
3. Migrated all 36 component MDX docs to Nextra format.
4. Built 3 Patterns pages (Checkout / Order detail / Dashboard) showing composed flows.
5. Built interactive Theming playground (live CSS-variable override with 5 presets).
6. Visual polish: Inter + JetBrains Mono via `next/font`, brand-accent rails on h2, denser sidebar, brand focus rings.
7. Deployed to Vercel, wired Vercel Web Analytics.
8. Solved **two-Reacts mismatch** (root has React 19, docs has React 18 — fixed with webpack alias + tsconfig path alias + .figma.tsx exclude).
9. Footer credits Giri Poonati with LinkedIn link.

---

## Active skills

Load and apply all rules from `.claude/skills/frontend.md` and `.claude/skills/design-system.md` for every component and token task.

---

## Mandatory workflow

1. **Fetch Figma context first.** Before writing any component, call `get_design_context` with the relevant node from the Figma file. Never build from memory or assumption.
2. **One component at a time.** Never start a second component while one is in progress.
3. **QA before moving on.** Run the component QA agent after every component is built. Do not proceed to the next component until QA passes.
4. **Token audit every 5 components.** After every 5th component, run the token audit agent to catch drift before it accumulates.

> **Note on Storybook tags**: The previous rule mandating `tags: ['autodocs']` on every story is **superseded** — Storybook v10 errors when both `autodocs` and a sibling `.mdx` exist. Current convention is `tags: []` on every story (matches the original Button.stories pattern).

---

## Folder structure

```
storefront-ds/
├── src/                                 ← Component library (Vite + Storybook)
│   ├── components/
│   │   ├── atoms/        # 12 atoms
│   │   ├── molecules/    # 15 molecules
│   │   └── organisms/    # 9 organisms
│   ├── tokens/           # All design tokens as TypeScript files
│   ├── index.css         # CSS variable definitions (source of truth)
│   └── stories/          # Cross-component MDX only
├── docs/                                ← Public documentation site (Next.js + Nextra)
│   ├── pages/
│   │   ├── _meta.tsx
│   │   ├── _app.tsx                     # Mounts Analytics + global fonts
│   │   ├── index.mdx                    # Landing
│   │   ├── use-cases.mdx
│   │   ├── install.mdx
│   │   ├── tokens.mdx
│   │   ├── principles.mdx
│   │   ├── theming.mdx
│   │   ├── components/                  # 36 MDX pages + _meta.tsx
│   │   └── patterns/                    # 3 MDX pages + _meta.tsx
│   ├── components/                      # Docs-only React (Preview, ColorSwatch, ThemePlayground, etc.)
│   ├── styles/globals.css               # Inlined token CSS (decoupled from src/index.css for Vercel)
│   ├── theme.config.tsx                 # Nextra theme — logo, footer, head, sidebar
│   ├── next.config.mjs                  # Webpack alias forces single React instance
│   └── tsconfig.json                    # Path alias for react/react-dom; excludes .figma.tsx
├── agents/                              # Component QA, Token Audit, Coverage Checker
└── .github/workflows/                   # GitHub Pages + Chromatic
```

Build in atomic order: **atoms → molecules → organisms**. Never build a molecule before all its atom dependencies exist.

---

## Component rules

### File naming

| File | Convention |
|---|---|
| Component | `ComponentName.tsx` |
| Story | `ComponentName.stories.tsx` |
| Types (if split) | `ComponentName.types.ts` |
| Index | `index.ts` |

### Props interface

Every component must export a typed props interface:

```ts
export interface ComponentNameProps {
  // required props first, optional last
}
```

Interface name must be `ComponentNameProps` — no exceptions.

### Type-only imports

Because the project uses `verbatimModuleSyntax`, every type-only import must use `import type`:

```ts
// ✅
import { forwardRef } from 'react';
import type { ButtonHTMLAttributes } from 'react';

// ❌
import { forwardRef, ButtonHTMLAttributes } from 'react';
```

### Required states

Every interactive component must implement all six states:

| State | Implementation |
|---|---|
| `default` | Base appearance |
| `hover` | `:hover` / `hover:` Tailwind variant |
| `focus` | `:focus-visible` / `focus-visible:` — never `:focus` alone |
| `active` | `:active` / `active:` Tailwind variant |
| `disabled` | `disabled` HTML attr + `disabled:` Tailwind variant + `pointer-events-none` |
| `error` | Prop-driven (`error?: boolean`) with error token colors |

### Commerce-specific states

Data-fetching and list components must additionally implement:

- **Loading skeleton** — use the `Skeleton` atom, never a centered spinner for layout components
- **Empty state** — use the `EmptyState` molecule with meaningful message + action CTA
- **Status indicators** — must use the `StatusBadge` molecule with one of the five mandated values:
  `pending` | `processing` | `completed` | `failed` | `cancelled`

### Currency / amount display

- Always use `AmountDisplay` or `PriceDisplay` — never format currency inline
- Respect locale and currency code props; never hardcode `$` or any symbol
- Handle `null` / `undefined` amounts gracefully (the components render `—` placeholder by default)

### Accessibility

- All interactive components must be fully keyboard accessible
- Use semantic HTML elements before reaching for `role=`
- Every icon-only button needs `aria-label`
- Focus rings must be visible — never `outline-none` without a `focus-visible:ring` replacement

---

## Token rules

### Naming convention

```
--storefront-{category}-{name}
```

Examples:
```css
--storefront-color-primary-600
--storefront-spacing-4
--storefront-typography-body-sm
--storefront-radius-md
--storefront-shadow-card
```

### Categories

| Category | Covers |
|---|---|
| `color` | All color primitives and semantic aliases |
| `spacing` | Padding, margin, gap values |
| `typography` | Font size, line height, weight, family |
| `radius` | Border radius |
| `shadow` | Box shadows |
| `transition` | Duration and easing |
| `z` | Z-index scale |

### Absolute rules

- **Never hardcode a hex value** — always reference a token
- **Never use arbitrary Tailwind values** like `w-[137px]`, `text-[13px]`, `bg-[#3b82f6]`
- **Never mix token naming conventions** within a file or PR
- All tokens live in `src/tokens/` — no inline token definitions in component files
- The docs site (`docs/styles/globals.css`) keeps a synced *copy* of the token CSS for Vercel build reasons. If you change a token in `src/index.css`, update `docs/styles/globals.css` too — or run the (TODO) pre-build sync script.

---

## Storybook rules

Every component must have a story file. The story must include:

```ts
// Required exports per component story file:
export default meta;      // Meta with title, component, tags: [], argTypes
export const Default;     // The baseline story
export const AllVariants; // render() showing all variant props side by side
export const AllSizes;    // render() showing all size props (if applicable)
export const AllStates;   // render() showing default, disabled, error, loading
```

- `tags: []` — empty (NOT `['autodocs']`) because the sibling `.mdx` file provides the docs page
- `layout: 'centered'` is the default; use `'fullscreen'` only for organism-level stories
- Use `decorators` when a story needs a specific container width or background context
- Story file lives in the same folder as the component (`atoms/Button/Button.stories.tsx`), not in `src/stories/`
- **Render-only stories on components with required props must include `args` with stub defaults** — otherwise `StoryObj<typeof meta>` type-checking fails. Pattern:
  ```tsx
  export const AllVariants: Story = {
    args: { items: [] },          // satisfies required props
    render: () => (<>...</>),     // render ignores args
  };
  ```

> `src/stories/` is reserved for cross-component and documentation MDX pages only.

---

## Nextra docs site rules

### Component imports inside MDX

MDX files import the real React components via relative paths:

```mdx
import { Button } from '../../../src/components/atoms/Button/Button';
```

Path-alias imports (`@storefront-ds/...`) also work via the tsconfig `paths` config.

### Live previews

Wrap component examples in `<Preview>` (or `<Preview padded>`) from `docs/components/Preview.tsx`. It applies a consistent surface, padding, and border that matches the docs aesthetic.

### MDX gotchas (Nextra v3 + MDX 3)

- **No `<` followed by a digit in body text** — MDX parses it as a JSX tag. Rephrase `(<1s)` to `(under 1s)`.
- **`_meta.tsx`** (TypeScript), not `_meta.json`. v3 dropped JSON support.

### React version discipline

The docs site uses React 18, the parent project uses React 19. **All `react` and `react-dom` imports must resolve to docs's single instance** to avoid `$$typeof` symbol mismatches that throw "Objects are not valid as a React child" at runtime.

Three guardrails are in place:
1. `next.config.mjs` webpack alias — forces `react`, `react-dom`, `react/jsx-runtime`, `react/jsx-dev-runtime` to `docs/node_modules/`
2. `tsconfig.json` `paths` aliases the same for type resolution
3. `tsconfig.json` `exclude` keeps `.figma.tsx` and `.stories.tsx` out of the docs type-check (they use the root's React types via `@figma/code-connect`)

If you see "Objects are not valid as a React child (found: object with keys {$$typeof, type, key, props, _owner, _store})" — the guardrail above has broken. Don't bypass it.

---

## What to never do

| Prohibited | Why |
|---|---|
| Hardcode hex values anywhere | Breaks theming; use tokens |
| Use arbitrary Tailwind values (`w-[137px]`) | Bypasses the token system |
| Build a component without its Storybook story | Component is not considered done without it |
| Start a new component before QA passes on the current one | Defects compound |
| Skip the token audit past the 5-component mark | Token drift is hard to refactor later |
| Use `:focus` without `:focus-visible` | Triggers on mouse click; bad UX |
| Format currency inline | Use `AmountDisplay`/`PriceDisplay` — consistency across the system |
| Leave an empty state blank | Always provide message + action |
| Add a 6th order status without design review | The system is built around exactly five lifecycle states |
| Mix naming conventions in the same PR | Causes token audit failures |
| Build molecules before their atom dependencies | Breaks the atomic dependency graph |
| Use `tags: ['autodocs']` on stories with a sibling `.mdx` | Storybook v10 errors |
| Import `react` without going through the docs webpack alias | Triggers the two-Reacts runtime error |

---

## Definition of done (per component)

- [ ] Figma context fetched before writing any code
- [ ] Props interface exported with correct name (`ComponentNameProps`)
- [ ] All six states implemented (default, hover, focus, active, disabled, error)
- [ ] Commerce states implemented where applicable (skeleton, empty, status)
- [ ] Zero hardcoded colors, spacing, or typography values
- [ ] Type-only imports use `import type`
- [ ] Storybook story with `Default`, `AllVariants`, `AllSizes`, `AllStates` exports
- [ ] `tags: []` on story meta (not `['autodocs']`)
- [ ] Render-only stories include `args` stub for required props
- [ ] Keyboard accessible (tab, enter/space, escape where applicable)
- [ ] QA agent run and passed
- [ ] Barrel export added to `index.ts`
- [ ] Token audit run (if this is the 5th component in the batch)
- [ ] Nextra MDX page created at `docs/pages/components/<name>.mdx`
- [ ] Component added to `docs/pages/components/_meta.tsx`

---

## Open work / roadmap

- Dark mode (token system is structured for it; visual pass needed)
- npm package distribution (currently distributed as source / git submodule)
- Mobile-first responsive variants (desktop-first today)
- i18n / RTL support (en-US only today)
- Wire `CHROMATIC_PROJECT_TOKEN` so the visual regression workflow goes green
- Update placeholder Figma node-ids on the newer 22 components' `.figma.tsx` files
- Pre-build script to regenerate `docs/styles/globals.css` from `src/index.css` so tokens stay in sync automatically
